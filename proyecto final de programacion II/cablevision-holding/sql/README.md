# Archivos SQL - Cablevisión Holding

Esta carpeta contiene los scripts SQL necesarios para crear e importar la base de datos del proyecto en diferentes sistemas gestores.

## Archivos disponibles

### 1. `cablevision_holding_export.sql`
- **Descripción**: Exportación directa desde la base de datos H2
- **Formato**: Sintaxis específica de H2 Database
- **Uso**: Archivo de respaldo y referencia
- **Compatible con**: H2 Database
- **Notas**: Incluye comandos específicos de H2 como `CREATE CACHED TABLE`, `GENERATED BY DEFAULT AS IDENTITY`, notación Unicode `U&'texto\00f3'`

### 2. `cablevision_holding_mysql.sql`
- **Descripción**: Script convertido y optimizado para MySQL/MariaDB
- **Formato**: Sintaxis MySQL 8.0+
- **Uso**: **Archivo principal para importar en MySQL Workbench**
- **Compatible con**: MySQL 8.0+, MariaDB 10.5+
- **Características**:
  - Crea la base de datos `cablevision_holding`
  - Define todas las tablas con relaciones
  - Incluye los datos actuales exportados desde H2
  - Vistas predefinidas para consultas rápidas
  - Índices para optimización de rendimiento
  - Consultas de verificación al final

### 3. `database-schema.sql`
- **Descripción**: Plantilla de diseño con estructura completa y datos de ejemplo adicionales
- **Formato**: Sintaxis MySQL
- **Uso**: Referencia de arquitectura y datos de prueba ampliados
- **Compatible con**: MySQL 8.0+, MariaDB 10.5+
- **Características**:
  - Esquema completo documentado
  - Datos de ejemplo más extensos (8 países, 6 áreas de mercado, 5 empresas)
  - Vistas útiles para reporting
  - Consultas de verificación

## ¿Cuál usar?

### Para presentar al profesor:
- **Los tres archivos** demuestran:
  - `cablevision_holding_export.sql`: Exportación original desde H2
  - `cablevision_holding_mysql.sql`: Conversión a MySQL con datos reales
  - `database-schema.sql`: Diseño completo con ejemplos ampliados

### Para trabajar con MySQL:
- **Usa `cablevision_holding_mysql.sql`**: Es el archivo listo para importar con tus datos actuales

### Para desarrollo/pruebas:
- **Usa `database-schema.sql`**: Tiene más datos de ejemplo para testing

## Cómo importar en MySQL

### Opción 1: MySQL Workbench (GUI)
```
1. Abrir MySQL Workbench
2. Conectarse al servidor
3. File > Open SQL Script > cablevision_holding_mysql.sql
4. Ejecutar todo (⚡ o Ctrl+Shift+Enter)
```

### Opción 2: Línea de comandos (PowerShell)
```powershell
# Importar el script completo
mysql -u root -p < sql\cablevision_holding_mysql.sql

# O si prefieres especificar la base de datos:
mysql -u root -p cablevision_holding < sql\cablevision_holding_mysql.sql
```

### Opción 3: Desde el proyecto con Maven
```powershell
# Configurar perfil mysql en application-mysql.properties
# Luego ejecutar la aplicación:
$env:SPRING_PROFILES_ACTIVE = 'mysql'; mvn spring-boot:run
```

## Verificación post-importación

Ejecutar en MySQL Workbench o CLI:
```sql
USE cablevision_holding;

-- Ver tablas creadas
SHOW TABLES;

-- Verificar registros
SELECT COUNT(*) AS total_usuarios FROM usuario;
SELECT COUNT(*) AS total_empresas FROM empresa;
SELECT COUNT(*) AS total_paises FROM pais;
SELECT COUNT(*) AS total_cuentas FROM cuentas;

-- Usar vistas predefinidas
SELECT * FROM vista_empresas;
SELECT * FROM vista_vendedores;
```

## Estructura de carpetas del proyecto

```
cablevision-holding/
├── data/                    # Base de datos H2 (archivos binarios)
│   ├── cablevision_holding.mv.db
│   └── cablevision_holding.trace.db
├── sql/                     # Scripts SQL (esta carpeta)
│   ├── cablevision_holding_export.sql
│   ├── cablevision_holding_mysql.sql
│   └── database-schema.sql
└── src/main/resources/
    ├── application.properties        # Configuración H2 (por defecto)
    └── application-mysql.properties  # Configuración MySQL
```

## 📊 Modelo de Datos y Relaciones

### Visión General del Modelo

El sistema gestiona un **holding de empresas de telecomunicaciones** con tres actores principales: **Vendedores**, **Asesores** y **Administradores**. Además, incluye un módulo de **sistema ATM** para gestión de cuentas bancarias.

---

### 🗂️ Tablas Principales

#### 1. **`usuario`** (Tabla base - Herencia)
**Propósito**: Almacenar información común de todos los usuarios del sistema.

**Campos principales**:
- `id` (PK): Identificador único
- `nombre`: Nombre completo del usuario
- `direccion`: Dirección física
- `dtype`: Discriminador para herencia ('Vendedor' o 'Asesor')

**Tipo**: Tabla padre en una estrategia de herencia JOINED (JPA).

**Por qué existe**: Implementa el patrón de herencia donde `Vendedor` y `Asesor` comparten atributos comunes pero tienen características específicas propias.

---

#### 2. **`vendedor`** (Hereda de `usuario`)
**Propósito**: Representar a los vendedores que trabajan para las empresas del holding.

**Campos principales**:
- `id` (PK, FK → usuario): Identificador heredado
- `codigo_vendedor`: Código único del vendedor (ej: 'CV001', 'FL001')
- `empresa_id` (FK → empresa): Empresa donde trabaja
- `vendedor_superior_id` (FK → vendedor): Vendedor que supervisa (jerarquía)

**Relaciones**:
- ➡️ **Pertenece a** una `empresa` (Many-to-One)
- ➡️ **Puede tener** un `vendedor_superior` (Self-Referential)
- ⬅️ **Puede supervisar** varios `vendedores` subordinados (One-to-Many)
- ⬅️ **Puede captar** otros vendedores mediante `captacion` (One-to-Many)

**Por qué existe**: Los vendedores son agentes de venta que operan bajo una estructura jerárquica. Pueden captar nuevos vendedores y reportan a un superior.

---

#### 3. **`asesor`** (Hereda de `usuario`)
**Propósito**: Representar a los asesores especializados que brindan consultoría a las empresas.

**Campos principales**:
- `id` (PK, FK → usuario): Identificador heredado
- `codigo_asesor`: Código único del asesor (ej: 'AS001')
- `titulacion`: Título académico o profesional (MBA, Doctorado, etc.)

**Relaciones**:
- ⬅️ **Asesora** a empresas en áreas específicas mediante `asesor_empresa_area` (One-to-Many)

**Por qué existe**: Los asesores son consultores externos que brindan expertise en áreas específicas del mercado (telecomunicaciones, streaming, fintech). A diferencia de los vendedores, no tienen estructura jerárquica.

---

#### 4. **`empresa`**
**Propósito**: Representar las empresas que forman parte del holding.

**Campos principales**:
- `id` (PK): Identificador único
- `nombre`: Nombre de la empresa (ej: 'Cablevisión Argentina')
- `fecha_entrada_holding`: Fecha de incorporación al holding
- `facturacion_anual`: Ingresos anuales en la moneda local
- `numero_vendedores`: Cantidad de vendedores registrados
- `ciudad_sede`: Ciudad principal de operaciones
- `pais_sede_id` (FK → pais): País donde tiene la sede principal

**Relaciones**:
- ➡️ **Tiene sede en** un `pais` principal (Many-to-One)
- ↔️ **Opera en** múltiples `pais` mediante `empresa_pais_operacion` (Many-to-Many)
- ↔️ **Trabaja en** múltiples `area_mercado` mediante `empresa_area_mercado` (Many-to-Many)
- ⬅️ **Emplea** a varios `vendedores` (One-to-Many)
- ⬅️ **Es asesorada por** `asesores` mediante `asesor_empresa_area` (One-to-Many)

**Por qué existe**: Las empresas son el núcleo del holding. Cada una tiene presencia en diferentes países y opera en distintas áreas del mercado (TV cable, internet, streaming).

---

#### 5. **`pais`**
**Propósito**: Almacenar información geográfica de los países donde operan las empresas.

**Campos principales**:
- `id` (PK): Identificador único
- `nombre`: Nombre del país
- `pib`: Producto Interno Bruto
- `numero_habitantes`: Población
- `capital`: Ciudad capital

**Relaciones**:
- ⬅️ **Es sede de** varias `empresas` (One-to-Many)
- ↔️ **Tiene operaciones de** múltiples `empresas` mediante `empresa_pais_operacion` (Many-to-Many)

**Por qué existe**: Permite análisis de mercado por región, evaluar oportunidades de expansión y entender el contexto económico donde operan las empresas.

---

#### 6. **`area_mercado`**
**Propósito**: Clasificar los diferentes segmentos de negocio en telecomunicaciones.

**Campos principales**:
- `id` (PK): Identificador único
- `nombre`: Nombre del área (ej: 'Telecomunicaciones', 'Streaming', 'Fintech')
- `descripcion`: Detalle del área de mercado

**Relaciones**:
- ↔️ **Es cubierta por** múltiples `empresas` mediante `empresa_area_mercado` (Many-to-Many)
- ⬅️ **Es asesorada por** `asesores` mediante `asesor_empresa_area` (One-to-Many)

**Por qué existe**: Permite especialización de empresas y asesores. Una empresa puede operar en TV por cable, internet y telefonía simultáneamente. Los asesores se especializan en áreas específicas.

---

### 🔗 Tablas de Relación (Many-to-Many)

#### 7. **`empresa_pais_operacion`**
**Propósito**: Relacionar empresas con los países donde operan (no necesariamente donde tienen la sede).

**Campos**:
- `empresa_id` (PK, FK → empresa)
- `pais_id` (PK, FK → pais)

**Ejemplo real**: 
- Cablevisión Argentina (sede en Argentina) también opera en Uruguay
- Brasil Connect (sede en Brasil) opera en Brasil, Argentina y Chile

**Por qué existe**: Las empresas de telecomunicaciones suelen tener presencia multinacional. Esta tabla permite rastrear la expansión geográfica.

---

#### 8. **`empresa_area_mercado`**
**Propósito**: Relacionar empresas con las áreas de mercado en las que operan.

**Campos**:
- `empresa_id` (PK, FK → empresa)
- `area_mercado_id` (PK, FK → area_mercado)

**Ejemplo real**:
- Cablevisión Argentina opera en: Telecomunicaciones, Internet y Telefonía Fija
- Brasil Connect opera en: Telecomunicaciones, Internet, Telefonía Móvil y Streaming

**Por qué existe**: Las empresas diversifican sus servicios. Una misma empresa puede ofrecer TV por cable, internet y telefonía móvil.

---

#### 9. **`asesor_empresa_area`**
**Propósito**: Asignar asesores a empresas específicas para áreas de mercado concretas.

**Campos**:
- `id` (PK): Identificador único de la asignación
- `asesor_id` (FK → asesor): Asesor asignado
- `empresa_id` (FK → empresa): Empresa que recibe asesoría
- `area_mercado_id` (FK → area_mercado): Área específica de consultoría
- `fecha_inicio`: Fecha de inicio de la asesoría

**Relaciones**:
- ➡️ **Involucra a** un `asesor` (Many-to-One)
- ➡️ **Es para** una `empresa` (Many-to-One)
- ➡️ **Se enfoca en** un `area_mercado` (Many-to-One)

**Ejemplo real**:
- Dr. Roberto Silva asesora a Cablevisión Argentina en Telecomunicaciones desde 2023-01-10
- Dra. Patricia Morales asesora a Flow Entertainment en Streaming y Entretenimiento desde 2023-03-20

**Por qué existe**: Los asesores son consultores especializados. Un mismo asesor puede trabajar con múltiples empresas en diferentes áreas. Esta tabla permite rastrear contratos de consultoría.

---

#### 10. **`captacion`**
**Propósito**: Registrar cuando un vendedor capta (recluta) a otro vendedor para la empresa.

**Campos**:
- `id` (PK): Identificador único
- `vendedor_id` (FK → vendedor): Vendedor que realiza la captación
- `vendedor_captado_id` (FK → vendedor): Vendedor que fue captado
- `fecha_captacion`: Fecha del reclutamiento

**Relaciones**:
- ➡️ **Es realizada por** un `vendedor` captador (Many-to-One)
- ➡️ **Involucra a** un `vendedor` captado (Many-to-One)

**Ejemplo real**:
- Carlos Rodríguez (CV001) captó a María González (CV002) el 2023-05-15
- Carlos Rodríguez (CV001) captó a Luis Pérez (CV003) el 2023-08-22

**Por qué existe**: Implementa un sistema de incentivos donde los vendedores pueden reclutar nuevos agentes. Permite calcular comisiones por captación y medir la efectividad de reclutamiento.

---

### 💰 Módulo ATM (Sistema Bancario)

#### 11. **`cuentas`**
**Propósito**: Gestionar cuentas bancarias para operaciones de cajeros automáticos.

**Campos principales**:
- `id` (PK): Identificador único
- `numero_cuenta`: Número de cuenta único
- `pin`: PIN encriptado (BCrypt)
- `saldo`: Saldo disponible (DECIMAL)
- `activa`: Estado de la cuenta (BOOLEAN)
- `fecha_creacion`, `ultima_actualizacion`: Auditoría

**Relaciones**:
- ⬅️ **Tiene** múltiples `transacciones` (One-to-Many)

**Por qué existe**: Las empresas del holding ofrecen servicios financieros complementarios. Permite a clientes realizar operaciones bancarias básicas.

---

#### 12. **`transacciones`**
**Propósito**: Registrar todas las operaciones realizadas sobre las cuentas.

**Campos principales**:
- `id` (PK): Identificador único
- `cuenta_id` (FK → cuentas): Cuenta involucrada
- `tipo`: Tipo de operación ('RETIRO', 'DEPOSITO', 'CONSULTA_SALDO')
- `monto`: Cantidad de dinero
- `fecha`: Timestamp de la operación
- `descripcion`: Detalle opcional
- `exitosa`: Si la transacción fue exitosa

**Relaciones**:
- ➡️ **Pertenece a** una `cuenta` (Many-to-One)

**Por qué existe**: Implementa auditoría completa de operaciones financieras. Permite generar estados de cuenta, detectar fraudes y cumplir con regulaciones bancarias.

---

#### 13. **`dispensadores`**
**Propósito**: Representar cajeros automáticos (ATMs) físicos.

**Campos principales**:
- `id` (PK): Identificador único
- `codigo`: Código del cajero (ej: 'ATM001')
- `saldo_disponible`: Efectivo disponible en el cajero
- `ultima_recarga`: Fecha de la última recarga de efectivo
- `activo`: Si el cajero está operativo
- `version`: Control de concurrencia optimista

**Por qué existe**: Gestiona la infraestructura física de cajeros. Permite controlar el inventario de efectivo, programar recargas y detectar cajeros que necesitan mantenimiento.

---

## 🎯 Diagrama de Relaciones Simplificado

```
┌─────────────────────────────────────────────────────────────────┐
│                         HOLDING                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────┐        ┌─────────┐          ┌──────────────┐       │
│  │  PAIS   │◄───────┤ EMPRESA ├────────► │ AREA_MERCADO │       │
│  └─────────┘  sede  └────┬────┘  opera   └──────────────┘       │
│       ▲                  │                       ▲              │
│       │                  │ emplea                │              │
│       │ opera            │                       │              │
│       │                  ▼                       │ asesora      │
│  ┌─────────────┐   ┌──────────┐          ┌──────────┐           │
│  │  EMPRESA_   │   │ VENDEDOR │          │  ASESOR  │           │
│  │  PAIS_      │   └─────┬────┘          └────┬─────┘           │
│  │  OPERACION  │         │ capta              │                 │
│  └─────────────┘         │                    │                 │
│                          ▼                    ▼                 │
│                    ┌────────────┐      ┌──────────────────┐     │
│                    │ CAPTACION  │      │ ASESOR_EMPRESA_  │     │
│                    └────────────┘      │     AREA         │     │
│                                        └──────────────────┘     │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                      MÓDULO ATM                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────┐         ┌───────────────┐                         │
│  │ CUENTAS  ├────────►│ TRANSACCIONES │                         │
│  └──────────┘  tiene  └───────────────┘                         │
│                                                                 │
│  ┌──────────────┐                                               │
│  │ DISPENSADORES│  (infraestructura física)                     │
│  └──────────────┘                                               │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔑 Conceptos Clave

### Herencia de Tablas (JPA JOINED Strategy)
- `usuario` es la tabla padre
- `vendedor` y `asesor` heredan de `usuario`
- Cada subtipo tiene su propia tabla con campos adicionales
- El campo `dtype` (si se usa tabla única) o las FK en tablas separadas distinguen el tipo

### Relaciones Many-to-Many
Se implementan con tablas intermedias:
- `empresa_pais_operacion` → Empresas operan en múltiples países
- `empresa_area_mercado` → Empresas trabajan en múltiples áreas

### Relaciones de Negocio
- **Jerarquía de vendedores**: Self-referential en `vendedor.vendedor_superior_id`
- **Captación de vendedores**: Tabla `captacion` registra el reclutamiento
- **Asesorías especializadas**: Tabla `asesor_empresa_area` con fecha de inicio

### Módulo Independiente
El sistema ATM (`cuentas`, `transacciones`, `dispensadores`) es un módulo separado que podría usarse independientemente del sistema de holding.

---

## Soporte

Para más detalles sobre cómo conectar la aplicación a MySQL, consultar el archivo `CONEXION.md` en la raíz del proyecto.
